spring:
  application:
    name: product-service

server:
  port: 8080

info:
  Service: Product Service
  Version: 1.0.0

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
